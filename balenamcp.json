{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "local>schubydoo/renovate-config"
  ],
  "postUpdateOptions": [
    "gomodTidy"
  ],
  "customManagers": [
    {
      "description": "Track GREMLINS_VERSION env var in the mutation testing workflows. gremlins is installed at CI runtime via `go install`, so it's neither in go.mod (gomod manager skips it) nor a GitHub Action uses: line (github-actions manager skips it). The regex below picks the literal `GREMLINS_VERSION: vX.Y.Z` line and Renovate cross-references it against go-gremlins/gremlins's GitHub releases.",
      "customType": "regex",
      "managerFilePatterns": [
        "/^\\.github/workflows/mutation(-pr)?\\.yml$/"
      ],
      "matchStrings": [
        "GREMLINS_VERSION:\\s+(?<currentValue>v\\d+\\.\\d+\\.\\d+)"
      ],
      "depNameTemplate": "go-gremlins/gremlins",
      "datasourceTemplate": "github-releases",
      "versioningTemplate": "semver"
    },
    {
      "description": "Track BALENA_CLI_VERSION in .balena-cli-version. balena-cli is shelled out at runtime, so it is invisible to the gomod and github-actions managers; this regex picks the literal `BALENA_CLI_VERSION=X.Y.Z` line and Renovate cross-references it against the balena-cli npm package, opening a PR (with the upstream changelog) on each new release to review for parameter drift.",
      "customType": "regex",
      "managerFilePatterns": [
        "/^\\.balena-cli-version$/"
      ],
      "matchStrings": [
        "BALENA_CLI_VERSION=(?<currentValue>\\d+\\.\\d+\\.\\d+)"
      ],
      "depNameTemplate": "balena-cli",
      "datasourceTemplate": "npm",
      "versioningTemplate": "npm"
    }
  ],
  "packageRules": [
    {
      "description": "Indirect gomod deps still get bumped — Go's transitive graph is part of the supply chain.",
      "matchManagers": [
        "gomod"
      ],
      "matchDepTypes": [
        "indirect"
      ],
      "enabled": true
    },
    {
      "description": "Gremlins CI-installed tool — keep on the github-actions cadence + labels since the version lives in workflow files.",
      "matchPackageNames": [
        "go-gremlins/gremlins"
      ],
      "groupName": "github-actions",
      "commitMessagePrefix": "chore(ci):",
      "semanticCommitType": "chore",
      "semanticCommitScope": "ci",
      "addLabels": [
        "github-actions",
        "ci"
      ],
      "automerge": true
    },
    {
      "description": "balena CLI drift watcher — surface each new balena-cli release as its OWN PR (never grouped) so it can be reviewed for parameter drift before merging. chore(deps) so it never triggers a release-please version bump; this only moves the tracking marker in .balena-cli-version.",
      "matchPackageNames": [
        "balena-cli"
      ],
      "matchManagers": [
        "custom.regex"
      ],
      "commitMessagePrefix": "chore(deps):",
      "commitMessageTopic": "balena CLI",
      "semanticCommitType": "chore",
      "semanticCommitScope": "deps",
      "addLabels": [
        "balena-cli"
      ]
    }
  ]
}
