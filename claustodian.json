{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "local>schubydoo/renovate-config"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Track the Cloudflare Wrangler CLI major pinned in the deploy job's `npx --yes wrangler@4 deploy`. It is invoked via npx at deploy time, so it is not a package.json dependency and neither the npm nor github-actions manager sees it. Renovate opens a PR when a new Wrangler major ships, to review before rolling the deploy tool forward.",
      "managerFilePatterns": [
        "/^\\.github/workflows/deploy-worker\\.yml$/"
      ],
      "matchStrings": [
        "wrangler@(?<currentValue>\\d+)"
      ],
      "depNameTemplate": "wrangler",
      "datasourceTemplate": "npm",
      "versioningTemplate": "npm"
    }
  ],
  "packageRules": [
    {
      "description": "Hold TypeScript on the 6.x major (still take 6.x minor/patch). TS 7 is the ground-up native (Go port) compiler; our toolchain isn't ready — typescript-eslint's latest still caps its peer at typescript <6.1.0, so `eslint .` breaks under TS 7. Re-enable (drop this rule) once typescript-eslint publishes a release whose typescript peer range includes 7.x.",
      "matchPackageNames": [
        "typescript"
      ],
      "matchUpdateTypes": [
        "major"
      ],
      "enabled": false
    }
  ]
}
