{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "local>schubydoo/renovate-config"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Track the OSV-Scanner binary version pinned in the scheduled scan. Renovate bumps OSV_VERSION; update OSV_SHA256 to match (the workflow's sha256sum -c fails loudly until you do — the hosted Renovate App can't recompute a binary checksum).",
      "managerFilePatterns": [
        "^\\.github/workflows/osv-scanner\\.yml$"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)\\s+OSV_VERSION:\\s*['\"](?<currentValue>v[0-9][^'\"]*)['\"]"
      ],
      "versioningTemplate": "semver"
    }
  ],
  "packageRules": [
    {
      "description": "Hold TypeScript on the 6.x major (still take 6.x minor/patch). TS 7 is the ground-up native (Go port) compiler; our toolchain isn't ready — typescript-eslint's latest still caps its peer at typescript <6.1.0, so `eslint .` breaks under TS 7. Re-enable (drop this rule) once typescript-eslint publishes a release whose typescript peer range includes 7.x.",
      "matchPackageNames": [
        "typescript"
      ],
      "matchUpdateTypes": [
        "major"
      ],
      "enabled": false
    }
  ]
}
